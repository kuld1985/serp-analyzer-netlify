cd /path/to/serp-analyzer-netlify
git init
git add .
git commit -m "SERP analyzer Netlify"
# create repo on GitHub, then:
git remote add origin https://github.com/yourusername/yourrepo.git
git push -u origin main
